import React from 'react';
import Search from '../Search';
const Header = () => {
  return (
    <header>
      <h1>Search for books</h1>
      <Search />
    </header>
  );
};

export default Header;
